export interface Provincias{
  id: string;
  nombre: string;
  distritoId: string;
  provinciaId?: string;
}

export interface Presbiterios{
    id: string;
    nombre: string;
    distritoId: string;
    provinciaId?: string;
}
